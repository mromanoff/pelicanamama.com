const path = require('path');

// DO NOT REMOVE: this is used by webstorm for autocompletion when importing with '~' prefix

module.exports = {
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
};
